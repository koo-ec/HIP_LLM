[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "HIPLLM"
version = "1.1.0"
description = "Failure-probability scoring and hierarchical imprecise reliability inference for LLMs"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Robab Aghazadeh Chakherlou" },
  { name = "Qing Guo" },
  { name = "Siddartha Khastgir" },
  { name = "Peter Popov" },
  { name = "Xiaoge Zhang" },
  { name = "Xingyu Zhao" },
]
keywords = ["reliability", "imprecise probability", "hierarchical Bayes", "LLM evaluation"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "numpy>=2.0",
  "pandas>=2.2",
  "matplotlib>=3.10",
  "scipy>=1.11",
  "PyYAML>=6.0",
]

[project.urls]
Homepage = "https://github.com/koo-ec/HIP_LLM"
Repository = "https://github.com/koo-ec/HIP_LLM"
Issues = "https://github.com/koo-ec/HIP_LLM/issues"
Documentation = "https://hipllm.readthedocs.io"
Publication = "https://doi.org/10.1016/j.ress.2026.112615"

[project.optional-dependencies]
test = ["pytest>=8.0", "pytest-timeout>=2.3", "nbformat>=5.10", "papermill>=2.6", "ipykernel>=6.29"]
live = ["openai>=1.40", "anthropic>=0.40", "datasets>=2.20"]
vertex = ["langchain-google-vertexai>=2.0.8,<4"]
profile = ["psutil>=5.9"]
docs = [
  "sphinx>=7.4,<9",
  "furo>=2024.8.6",
  "sphinx-autodoc-typehints>=2.3",
  "myst-parser>=3.0",
]
dev = ["ruff>=0.6", "build>=1.2", "twine>=5.1"]
all = [
  "pytest>=8.0", "pytest-timeout>=2.3", "nbformat>=5.10", "papermill>=2.6",
  "ipykernel>=6.29", "openai>=1.40", "anthropic>=0.40", "datasets>=2.20",
  "psutil>=5.9", "ruff>=0.6", "build>=1.2", "twine>=5.1",
  "langchain-google-vertexai>=2.0.8,<4", "sphinx>=7.4,<9", "furo>=2024.8.6",
  "sphinx-autodoc-typehints>=2.3", "myst-parser>=3.0",
]

[dependency-groups]
dev = ["ruff>=0.6", "build>=1.2", "twine>=5.1", "pytest>=8.0", "pytest-timeout>=2.3"]
docs = ["sphinx>=7.4,<9", "furo>=2024.8.6", "sphinx-autodoc-typehints>=2.3", "myst-parser>=3.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["hip_llm*", "HIPLLM*"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
  "live: hits real provider APIs and costs money",
  "slow: long-running numerical convergence or scalability checks",
  "notebook: executes the replication notebook end to end",
]
filterwarnings = [
  "error::RuntimeWarning",
  "ignore::DeprecationWarning",
  "ignore::PendingDeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["HIP_LLM_exact_replication.ipynb"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
